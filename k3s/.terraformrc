# Required for local module development
# Mirror path must contain a build go package
provider_installation {
filesystem_mirror {
    path    = "path-to-go/GO/src/github.com"
  }
  direct {
    exclude = ["terraform.local/*/*"]
  }
}
